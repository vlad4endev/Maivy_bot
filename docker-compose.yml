services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Медиафайлы можно монтировать без пересборки образа
      - ./assets:/app/assets:ro
