# Скопируйте на сервер: cp docker-compose.override.example.yml docker-compose.override.yml
# Файл в .gitignore — git pull не конфликтует с вашими настройками.
#
# Пример: другой порт webhook на сервере
# services:
#   bot:
#     ports:
#       - "3001:3001"
#     environment:
#       WEBHOOK_PORT: "3001"
#       TELEGRAM_WEBHOOK_URL: "https://your-domain.com/telegram/webhook"
#
# Обязательно в .env на сервере (не коммитить):
#   BOT_API_SECRET=...
#   BOT_SLUG=maivy

services: {}
